package com;

import com.benito.PCcomponentes.Procesador;
import com.benito.PCcomponentes.TarjetaGrafica;

public class Main {
    public static void main(String[] args) {
        System.out.println("**PCComponentes**");

        Procesador amd = new Procesador();
        amd.setId(65);

        System.out.println(amd.getId());

        TarjetaGrafica ati = new TarjetaGrafica();
        ati.setId(1);

        System.out.println(ati.getId();

        SetupPC setupPC = new SetupPC():
        setupPC setId("ASDFG123")
                setupPC.setProcesador(amd):

        System.out.println(setupPC.getProcesador())

    }
}